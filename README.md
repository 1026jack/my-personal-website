# Personal Website

A simple personal homepage built with React and Vite for GitHub Pages.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

## GitHub Pages deployment

This project includes a GitHub Actions workflow in `.github/workflows/deploy.yml`.
After pushing the repository to GitHub, the workflow can publish the `dist` folder
to GitHub Pages automatically.
